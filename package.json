{
  "name": "node",
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/ws": "^8.18.1",
    "serve": "^14.2.5"
  },
  "dependencies": {
    "ws": "^8.19.0"
  },
  "scripts": {
    "build": "tsc && mkdir -p dist/styles && cp src/front/styles/*.css dist/styles/",
    "watch": "tsc --watch",
    "serve:front": "serve . --no-clipboard -s",
    "serve:server": "node dist/server/index.js"
  }
}
